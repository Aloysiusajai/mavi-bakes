export default function ProfilePage() {
  return (
    <main className="min-h-screen flex items-center justify-center" style={{ background: "var(--background)" }}>
      <div className="p-8 glass rounded-lg max-w-3xl">
        <h1 className="text-3xl font-serif mb-4" style={{ color: "var(--foreground)" }}>Profile</h1>
        <p style={{ color: "var(--color-chocolate)" }}>This is a placeholder user profile page. Replace with your user dashboard.</p>
      </div>
    </main>
  );
}
